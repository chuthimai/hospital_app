import 'package:flutter/material.dart';

class ViewTransferLetterDetailScreen extends StatelessWidget {
  const ViewTransferLetterDetailScreen({super.key});


  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: const Text('ViewTransferLetterDetailScreen'),
        ),
      ),
    );
  }
}