import 'shift.dart';

class WorkSchedule {
  final int id;
  final DateTime date;
  final Shift shift;

  WorkSchedule({
    required this.id,
    required this.date,
    required this.shift,
  });
}
