import 'package:flutter/material.dart';

class ViewFollowupAppointmentDetailScreen extends StatelessWidget {
  const ViewFollowupAppointmentDetailScreen({super.key});


  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: const Text('ViewFollowupAppointmentDetailScreen'),
        ),
      ),
    );
  }
}